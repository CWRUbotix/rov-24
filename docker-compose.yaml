version: '3.9'
services:
  ros:
    build: .
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - ~/.ssh:/home/user/.ssh:ro
      - /tmp/.X11-unix:/tmp/.X11-unix
    tty: true
    stdin_open: true