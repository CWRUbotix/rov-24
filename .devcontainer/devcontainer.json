{
    "name" : "ROV",
    "dockerComposeFile": ["../docker-compose.yaml"],
    "service": "ros",
    "workspaceFolder": "/rov-24",
    "shutdownAction": "stopCompose",
    "overrideCommand": false,
    // "postStartCommand": "export PYTHONWARNINGS=ignore:::setuptools.command.install,ignore:::setuptools.command.easy_install,ignore:::pkg_resources && . /rov-24/install/setup.sh"
    "customizations": {
        "vscode" : {
            "extensions": ["ms-python.flake8"]
        }
    }
}