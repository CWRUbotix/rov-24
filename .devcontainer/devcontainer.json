{
    "name" : "ROV",
    "dockerComposeFile": ["../docker-compose.yaml"],
    "service": "ros",
    "workspaceFolder": "/rov-24",
    "shutdownAction": "stopCompose",
    "overrideCommand": false,
    // "postStartCommand": ". install/setup.sh && export PYTHONWARNINGS=ignore:::setuptools.command.install,ignore:::setuptools.command.easy_install,ignore:::pkg_resources",
    "customizations": {
        "vscode" : {
            "extensions": ["ms-python.flake8", "ms-azuretools.vscode-docker"]
        }
    }
}